# CONVENTUS_KEY injiceres af deploy-pipeline (se deploy.yml)
# Må IKKE committes med en rigtig nøgle
SetEnv CONVENTUS_KEY CONVENTUS_KEY_PLACEHOLDER

# Beskyt følsomme filer mod direkte HTTP-adgang
<FilesMatch "(^\.|\.(json|key|pem)$)">
    Order allow,deny
    Deny from all
</FilesMatch>
